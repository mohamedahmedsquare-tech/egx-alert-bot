# بوت أسعار أسهم EGX + تنبيهات تليجرام (نسخة Local + Cloud معاً)

الفكرة: نشغّل نسختين من نفس البوت — واحدة على جهازك (Local) وواحدة على
GitHub Actions (Cloud) — بحيث لو جهازك اتقفل أو النت قطع، الـ Cloud فاضل
شغال ومتابع لوحده.

**ملحوظة:** لو الاتنين شغالين في نفس الوقت ولقوا نفس السهم داخل منطقة
الشراء، هتوصلك رسالتين متطابقتين تقريبًا (وحدة من كل نسخة) — ده طبيعي
ومش خطأ، وأمان إضافي أحسن من إشعار واحد ممكن يضيع.

## الملفات

- `egx_price_alert.py` — المنطق الأساسي (بيتستخدم من الاتنين)
- `local_runner.py` — نسخة الجهاز: بتفضل شغالة في لووب طول ما اللابتوب مفتوح
- `.env.example` — مثال لملف الإعدادات المحلي (لازم تعمله `.env` وتحط فيه بياناتك)
- `.github/workflows/egx_alert.yml` — جدولة الـ Cloud (GitHub Actions)
- `requirements.txt` — المكتبات المطلوبة

---

## الإعداد المشترك (خطوة أولى لازمة للاتنين)

### 1) اعمل بوت تليجرام
1. افتح تليجرام وابحث عن **@BotFather**
2. ابعتله `/newbot` واتبع التعليمات
3. هيديك **token** شكله كده: `123456789:ABCdefGhIJKlmNoPQRstuVWXyz`

### 2) هات الـ Chat ID بتاعك
1. ابعت أي رسالة للبوت اللي عملته
2. افتح في المتصفح: `https://api.telegram.org/bot<TOKEN>/getUpdates`
3. هتلاقي رقم جنب `"chat":{"id": ...}` — ده الـ chat id

---

## النسخة Local (على جهازك)

1. نزّل Python (3.10+) لو مش موجود
2. افتح Terminal/CMD في مجلد المشروع وثبّت المكتبات:
   ```
   pip install -r requirements.txt
   ```
3. اعمل نسخة من `.env.example` باسم `.env` واملأ بياناتك:
   ```
   TELEGRAM_BOT_TOKEN=التوكن_بتاعك
   TELEGRAM_CHAT_ID=الآيدي_بتاعك
   ```
4. شغّل:
   ```
   python local_runner.py
   ```
   السكريبت هيفضل شغال، بيشيك كل 30 دقيقة أثناء ساعات التداول
   (الأحد-الخميس، 10:00-2:30 بتوقيت القاهرة)، ولو السوق مقفول بينام
   ويشيك كل 15 دقيقة هل فتح ولا لأ.

5. **عشان يفضل شغال حتى لو قفلت الشاشة (اختياري):**
   - **Windows**: استخدم Task Scheduler عشان يشغّل السكريبت مع بداية الجهاز
   - **Mac/Linux**: استخدم `nohup python local_runner.py &` أو اعمله كـ
     launchd/systemd service

---

## النسخة Cloud (GitHub Actions)

1. اعمل حساب مجاني على github.com لو معندكش
2. اعمل ريبو جديد (Private أفضل للخصوصية)
3. ارفع كل الملفات بحيث يبقى الشكل كده بالظبط:
   ```
   your-repo/
   ├── egx_price_alert.py
   ├── requirements.txt
   └── .github/
       └── workflows/
           └── egx_alert.yml
   ```
   (متحتاجش ترفع `local_runner.py` أو `.env` — ده خاص بالنسخة المحلية بس،
   ولو رفعت `.env` بالغلط هتبان بياناتك للجميع لو الريبو Public)

4. ضيف الـ Secrets: Settings → Secrets and variables → Actions → New repository secret
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`

5. خلاص — هيشتغل تلقائي. تقدر تجربه يدوي من تبويب Actions → Run workflow

---

## تعديل قائمة المتابعة ومناطق الشراء

افتح `egx_price_alert.py` وعدّل:

```python
WATCHLIST = {
    "SWDY.CA": (105, 115, "السويدي إليكتريك"),
    "TMGH.CA": (90, 97, "طلعت مصطفى"),
    "FWRY.CA": (17, 18.5, "فوري"),
    "ETEL.CA": (112, 120, "المصرية للاتصالات"),
    "ADIB.CA": (46, 50, "أبوظبي الإسلامي مصر"),
}
```
الأسهم المصرية على Yahoo Finance بتاخد لاحقة `.CA`. عدّل الملف مرة واحدة
وحدّثه في المكانين (جهازك والريبو) عشان يفضلوا متطابقين.

## ملاحظات
- Yahoo Finance أحيانًا فيه تأخير بسيط في أسعار EGX — مناسب كمؤشر أولي مش تنفيذ فوري
- GitHub Actions مجاني (٢٠٠٠ دقيقة شهريًا للريبوهات الخاصة) وكافي جدًا للاستخدام ده
